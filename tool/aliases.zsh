# 在仓库任意目录执行：
#   source /absolute/path/to/fcmp/tool/aliases.zsh
# 或者在仓库根执行：
#   source tool/aliases.zsh
#
# 建议写进 ~/.zshrc（按需改成你的仓库实际路径）：
#   [[ -f "$HOME/personal/flutter/fcmp/tool/aliases.zsh" ]] && source "$HOME/personal/flutter/fcmp/tool/aliases.zsh"

alias melos='fvm dart run melos'
alias mm='fvm dart run melos'

