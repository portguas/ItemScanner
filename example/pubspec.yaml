name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Internal packages
  logging_util:
    path: ../packages/logging_util
  ui_design_system:
    path: ../packages/ui_design_system
  service_network:
    path: ../packages/service_network

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
